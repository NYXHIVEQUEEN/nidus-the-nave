# NIDUS SAVE — 2026-08-28 pre-grown-annex stamp

Package: NYXHIVEQUEEN/nidus `main` @ `f51957b`.
**Save taken before the 10 grown-hive watch improvements.**

What is saved: source hive at cathedral + socket collars / print dart / watchNave.
What is not touched: player keys
`nidus.save.v1` + `.bak` + `nidus.slot.*` + `nidus.prefs.v1`.

`sim.ts` / `save.ts` / `store.ts` / `content.ts` byte-identical this stamp.
No wipe. No NEW HIVE. Nested unlocks stay gated.

This file is the rollback point if the grown-annex pass misbehaves.

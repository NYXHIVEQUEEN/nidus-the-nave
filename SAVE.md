# NIDUS SAVE — 2026-09-01 pre-pass (watchable 10)

Pre-pass stamp **before** the return / look-at / purpose / juice overhaul.

- Local HEAD: `11dd712` (Export from Grok)
- Package repo: `NYXHIVEQUEEN/nidus` `main` @ `c8a857d`
- What is saved: cathedral nave + grown annexes + tendons/sap + nested unlocks
- What is not touched: player keys `nidus.save.v1` + `.bak` + `nidus.slot.*` + `nidus.prefs.v1`

`sim.ts` / `save.ts` / `store.ts` / `content.ts` / `progress.ts` / `types.ts` byte-identical this stamp.

No wipe. No NEW HIVE. Nested unlocks stay gated.

This file is the rollback point if the watchable pass misbehaves.
